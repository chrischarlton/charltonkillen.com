---
layout: page.njk
title: Thoughts
description: Notes on technology leadership, real-world agile, business transformation, and the valuable lessons that come from failure.
---

<div class="page-content">
  <h1>Thoughts</h1>
  <p>Notes on technology leadership, real-world agile, business transformation, and the valuable lessons that come from failure.</p>
</div>

<div class="thoughts-grid">
  {%- for thought in collections.thoughts | reverse -%}
    <a href="{{ pathPrefix }}{{ thought.url }}" class="thought-card"
       {% if thought.data.color %}data-color="{{ thought.data.color }}" style="--story-color: {{ thought.data.color }}; border-color: {{ thought.data.color }};"{% endif %}>
      <div class="thought-visual {% if thought.data.image %}has-image{% else %}colour-only{% endif %}" 
           {% if thought.data.color %}style="background-color: {{ thought.data.color }};"{% endif %}>
        {% if thought.data.image %}
          <img src="{{ pathPrefix }}{{ thought.data.image }}" alt="{{ thought.data.title }}" loading="lazy">
        {% endif %}
      </div>
      <div class="thought-content">
        <div class="thought-date">{{ thought.date | dateFormat }}</div>        
        <h2 class="thought-title">{{ thought.data.title }}</h2>
        {% if thought.data.description %}
          <p class="thought-excerpt">{{ thought.data.description }}</p>
        {% endif %}
      </div>
    </a>
  {%- endfor -%}
</div>
